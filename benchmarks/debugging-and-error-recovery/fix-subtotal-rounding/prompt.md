# Task: Fix incorrect invoice totals

The invoice subtotal is wrong for fractional line items. Reproduce the failure with `node benchmark-app/invoice.test.js`, identify the root cause, implement the smallest correct fix in `benchmark-app`, and rerun that test. Do not inspect or modify unrelated repository files.
