# Task: Add Lazygit configuration

Add Lazygit to this dotfiles repository. Install it through Homebrew and manage its configuration as a Stow package.

Configure Lazygit to show icons and use Neovim as its editor. Update the repository documentation so the package is discoverable. Follow the repository's conventions for portable, non-secret configuration.
